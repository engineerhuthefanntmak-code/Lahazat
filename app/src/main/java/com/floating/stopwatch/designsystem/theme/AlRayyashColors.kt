package com.floating.stopwatch.designsystem.theme

import androidx.compose.ui.graphics.Color

object AlRayyashColors {
    // Primary Surface / Background Colors (Deep Black / Dark Atmosphere)
    val BackgroundDeep = Color(0xFF050505)
    val SurfaceDeep = Color(0xFF0D0D0D)
    val SurfaceElevated = Color(0xFF141414)
    val SurfaceBorder = Color(0xFF222222)

    // Primary Text Colors
    val TextPrimary = Color(0xFFF5F3EF)      // Warm Creamy White
    val TextSecondary = Color(0xFF8A8680)    // Warm Gray
    val TextMuted = Color(0xFF5A5752)        // Subtle Muted Gray

    // Accent Colors (Luxury Gold Accent)
    val GoldAccent = Color(0xFFC9A66B)       // Quiet Luxury Gold
    val GoldAccentLight = Color(0xFFE2C896)  // Muted Gold Light
    val GoldAccentDark = Color(0xFF8C6F3B)   // Muted Gold Dark
    val GoldSurface = Color(0xFF1C1810)      // Low opacity Gold Surface

    // Functional State Colors
    val Success = Color(0xFF6B9E78)
    val Error = Color(0xFFB85252)
}
